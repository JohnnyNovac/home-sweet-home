spring:
  application:
    name: presence-service
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: ${RABBITMQ_DEFAULT_USER}
    password: ${RABBITMQ_DEFAULT_PASS}
  mongodb:
    uri: mongodb://${PRESENCE_MONGO_USER}:${PRESENCE_MONGO_PASS}@mongodb:27017/presence?replicaSet=rs0&serverSelectionTimeoutMS=3000&connectTimeoutMS=3000&socketTimeoutMS=5000
  grpc:
    client:
      channels:
        yandex-service:
          address: yandex-service:9090

server:
  port: 8082

app:
  rabbitmq:
    presence-data-queue: presence-data
    illuminance-data-queue: presence-illuminance
    exchange: amq.topic
    command-key-prefix: home.cmd.
  grpc:
    lamp-deadline: 12s
  lamp:
    illuminance-threshold: 50
    lamp-off-delay: 15s
  measurements:
    radar-presence:
      name: radarPresence
    pir-sensor-presence:
      name: pirSensorPresence
    illuminance:
      name: illuminance
  device:
    command:
      measure: MEASURE
management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus
  endpoint:
    health:
      show-details: always
  metrics:
    tags:
      application: ${spring.application.name}
